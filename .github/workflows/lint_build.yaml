name: Lint and build

on: 
  pull_request:
    branches:
      - '**'             
  
  push:
    branches:
    - main

jobs:
  lint:
    uses: MAGIC-SCAN/User_Studies/.github/workflows/lint.yaml@main